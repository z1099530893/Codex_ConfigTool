# 项目计划

## 当前里程碑：v1.4.0 源码与发布重建

| 节点 | 依赖 | 输出与验收 | 状态 |
| --- | --- | --- | --- |
| P-007A 源码与文档整理 | P-006J、CR-007 | 清理发布说明中的过时行为，更新 7 张正式界面截图，排除本地误展开目录 | DONE |
| P-007B 发布候选验证 | P-007A | 118 项测试、差异检查、双包构建、版本/资源/隔离启动和 SHA-256 核验 | DONE |
| P-007C GitHub 发布事务 | P-007B、GitHub 登录 | 提交并快进推送 `main`，移动 `v1.4.0` 到发布提交，替换两个资产并更新发布说明 | DONE |

依赖图：P-006J → P-007A → P-007B → P-007C。

活动路径：v1.4.0 发布后真实 Codex 工作流复测。

保留对象：`v1.0.0` 至 `v1.3.0` 的标签、Release、说明和资产全部不变；`main` 不做强制推送或历史重写。

回滚点：旧 `v1.4.0` 标签提交 `12b5086c11d40d1a7b70b0ffddedab59e6c834ec`，旧 Release ID `377653760` 和两个旧资产 ID `533361707`、`533361556`。

相关记录：CR-008、DEV-031、TEST-007。

## 当前里程碑：每配置模型目录与 Codex 内切换预览版

| 节点 | 依赖 | 输出与验收 | 状态 |
| --- | --- | --- | --- |
| P-006A 完整模型目录 | 当前配置字段级写入 | 获取列表后按配置完整持久化；再次获取覆盖；手动输入模型可追加；不覆盖用户外部目录 | DONE |
| P-006B 三页面统一交互 | P-006A | 曾让当前、新建、编辑页面共用下拉与获取模型交互；当前页部分已由 P-006J 替代 | SUPERSEDED |
| P-006C 公开默认值同步、切换与回滚 | P-006A | 身份一致才同步公开启动默认值；恢复目标启动默认模型、默认推理强度和 owned/none/external 目录状态；不读取私有线程状态；失败恢复三文件 | DONE |
| P-006D 预览便携包 | P-006B、P-006C | 语法检查、84 项测试、单文件构建、隔离启动冒烟 | DONE |
| P-006E 客户真实 Codex 验收 | P-006D | A→B→A 恢复各配置启动默认值；Codex 对话内临时切换不覆盖配置库；新对话默认模型和既有对话行为复测 | READY |
| P-006F 实机故障返工 | P-006E | 修复运行中切换导致旧进程覆盖目标 API Key；模型目录提供低/中/高/超高选项并兼容旧目录；87 项测试和新预览包通过 | RETEST |
| P-006G 原生模型元数据优先与默认值语义修正 | P-006F | 官方 OpenAI Provider 不使用自有目录；自定义 Provider 精确复用 Codex 原生条目，未匹配模型安全降级；界面和文档明确启动默认值边界；96 项测试和新预览包通过 | RETEST |
| P-006H 手动 Codex 生命周期切换 | P-006G | 客户确认改回自动生命周期事务并移除独立重启入口 | SUPERSEDED |
| P-006I 自动生命周期切换事务 | P-006H、P-004E | 用户确认后正常退出；退出后同步旧配置、投影目标并启动；重复触发锁定；失败回滚；独立重启入口移除 | RETEST |
| P-006J 统一保存与双击应用 | P-006I | 当前页模型只读；新增/编辑单一保存且弹窗不超过主界面；完整模型列表只在保存时落盘；活动编辑使用待应用标记；双击当前配置可启动或应用，运行且无修改时不重启；OpenAI 原生配置不写入助手模型目录 | RETEST |

依赖图：P-006A → P-006B + P-006C → P-006D → P-006E → P-006F → P-006G → P-006H（被替代）→ P-006I → P-006J

活动路径：P-006J 最终自动验证、预览便携包构建与客户真实 Codex 复测。

明确排除：不实现协议转换、常驻代理、多供应商自动路由；不修改或删除聊天记录；不读取 Codex 私有应用数据库；不从对话内临时选择反向覆盖配置库；不强制覆盖既有对话的会话级模型。

回滚点：Git 提交 `12b5086c11d40d1a7b70b0ffddedab59e6c834ec`，以及仓库外文件级副本（见 `BACKUP_MANIFEST.md`）。生成产物位于忽略的 `dist/`。

相关记录：CR-006、BUG-004、DEV-012、DEV-014、DEV-015、DEV-016、DEV-017、DEV-018、DEV-019、DEV-020、DEV-026、DEV-027、TEST-006。

## 上一里程碑：自动更新与重启稳定性

| 节点 | 依赖 | 输出与验收 | 状态 |
| --- | --- | --- | --- |
| P-004A 自动更新检测 | GitHub Release 可访问 | 启动后台检查、关于页手动检查、新版本红点、失败静默、无自动下载或替换 | DONE |
| P-004B Codex 自动关闭与重启图标修复 | Windows Store 应用身份可发现 | 旧实现使用窗口关闭和超时强制结束，客户复测仍偶发缺失托盘图标 | SUPERSEDED |
| P-004D 原半自动安全重启 | P-004B 客户复测结论 | 用户从托盘正常退出后由配置助手启动；客户拒绝半自动流程 | SUPERSEDED |
| P-004E Windows 原生入口自动重启 | P-004D 客户反馈、Cockpit Tools 源码研究 | AppX 清单真实 AUMID；非强制正常退出；新主进程与窗口确认；系统托盘实机复测 | RETEST |
| P-004C 集成验收 | P-004A、P-004E | 语法、完整测试、单文件打包、手动更新检查、真实重启图标验证 | IN-PROGRESS |

活动路径：P-004B → P-004D（被替代）→ P-004E → P-004C（剩余：自动重启的真实系统托盘复测）。

回滚点：Git 提交 `4d364701e282d1c8bb6e12c713daa5cee55fb3e8`。本里程碑不修改用户配置、API Key、聊天记录或系统权限。

相关记录：CR-004、BUG-004、DEV-004、DEV-006、DEV-007、DEV-020、DEV-021、TEST-004

## 当前里程碑：Windows 安装与后续一键更新

| 节点 | 依赖 | 输出与验收 | 状态 |
| --- | --- | --- | --- |
| P-005A 安装包定义 | 现有 PyInstaller 单文件构建 | 当前用户安装、默认可取消的桌面快捷方式、开始菜单、卸载入口、用户数据隔离 | DONE |
| P-005B 双产物构建 | P-005A、Inno Setup 6 | 版本化 Portable 与 Setup EXE，版本和内容一致 | DONE |
| P-005C 安装链路验收 | P-005B | 首次安装、覆盖安装、卸载、快捷方式选择和用户数据保留实测 | DONE |
| P-005D 一键安装更新 | P-005C | 已确认不实现自动下载；保留红点提示和手动打开 Release | CANCELLED |

活动路径：P-005C → P-005D

明确排除：本节点不静默安装、不默认提升管理员权限、不删除用户 Codex 数据、不取消便携版。

回滚点：Git 提交 `4d364701e282d1c8bb6e12c713daa5cee55fb3e8` 及当前未提交工作区。安装包生成物位于忽略的 `dist/`，不作为源码回滚点。

相关记录：CR-005、DEV-008、TEST-005
